# Publish Changes

1. Run `git status` and `git diff` to review all pending changes
2. Stage only the relevant files (never use `git add -A` blindly — check for sensitive files first)
3. Write a concise commit message in Portuguese describing what changed and why
4. Commit and push to origin/main
5. Report the commit hash and a one-line summary of what was published
